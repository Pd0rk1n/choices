# === ESSENTIAL DEV TOOLS ===
base-devel
linux-headers
bash-completion
man-db
man-pages
git
nano

# === AUR HELPER ===
# yay (manual AUR install)
# git clone https://aur.archlinux.org/yay.git && cd yay && makepkg -si

# === X11 DISPLAY SERVER (required for startx) ===
xorg
xorg-xinit
xterm          # fallback terminal for X if others fail

# === DISPLAY MANAGER (GUI login, optional) ===
lightdm
lightdm-gtk-greeter
# OR:
# sddm

# === DESKTOP ENVIRONMENT or WINDOW MANAGER ===
# --- XFCE (recommended if fallback to startx needed) ---
xfce4
xfce4-goodies

# OR:
# --- Qtile (for WM users) ---
# qtile

# OR:
# --- GNOME (not ideal with startx fallback) ---
# gnome
# gnome-tweaks

# === AUDIO (you said it's preinstalled) ===
# pipewire is already installed via archinstall audio profile

# === TERMINAL & FONTS ===
xfce4-terminal
ttf-dejavu
ttf-liberation
noto-fonts
noto-fonts-cjk
noto-fonts-emoji

# === SYSTEM UTILITIES ===
htop
neofetch
lsd
unzip
unrar
zip
p7zip
rsync
gvfs
gvfs-smb
gvfs-mtp
ntfs-3g
dosfstools
exfatprogs
xdg-user-dirs
xdg-utils

# === BROWSERS ===
firefox
# brave-bin (AUR)
# google-chrome (AUR)

# === GAMING STACK ===


# === DEVELOPMENT TOOLS ===
gcc
make
cmake
python
python-pip
nodejs
npm


# === SYSTEM MANAGEMENT ===
reflector
pacman-contrib
logrotate
 sdd
